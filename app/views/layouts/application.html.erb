<!DOCTYPE html>
<html lang="en" class="">
  <head>
    <title>CraftBrewery</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= canonical_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="flex flex-col min-h-screen pt-20 bg-gradient-to-b from-zinc-100 via-zinc-200 to-zinc-100">
    <header class="fixed top-0 left-0 z-50 flex items-center justify-between w-full h-20 px-4 text-white bg-gradient-to-b from-gray-600 to-gray-900">
      <div>
        <%= link_to root_path, class: 'font-bold' do %>
          <%= inline_svg 'brewley', class: 'h-10 fill-white' %>
        <% end %>
      </div>
      <nav aria-label="main site navigation" class="">
        <ul class="flex space-x-4">
          <li class="rounded px-1 hover:bg-white transition-colors duration-300 <%= action_name == 'index' && controller_name == 'static_pages' ? 'bg-white text-black' : 'hover:bg-opacity-20'%>">
            <%= link_to 'Home', root_path %>
          </li>
          <li class="rounded px-1 hover:bg-white transition-colors duration-300 <%= controller_name == 'categories' ? 'bg-white text-black' : 'hover:bg-opacity-20'%>">
            <%= link_to 'Beer Categories', categories_path %>
          </li>
          <li class="rounded px-1 hover:bg-white transition-colors duration-300 <%= controller_name == 'products' ? 'bg-white text-black' : 'hover:bg-opacity-20'%>">
            <%= link_to 'Products', products_path %>
          </li>
          <li class="rounded px-1 hover:bg-white transition-colors duration-300 <%= controller_name == 'events' ? 'bg-white text-black' : 'hover:bg-opacity-20'%>">
            <%= link_to 'Events', events_path %>
          </li>
          <li class="rounded px-1 hover:bg-white transition-colors duration-300 <%= controller_name == 'blogs' ? 'bg-white text-black' : 'hover:bg-opacity-20'%>">
            <%= link_to 'Blog', blogs_path %>
          </li>
          <li class="rounded px-1 hover:bg-white transition-colors duration-300 <%= action_name == 'about' && controller_name == 'static_pages' ? 'bg-white text-black' : 'hover:bg-opacity-20'%>">
            <%= link_to 'About', about_path %>
          </li>
          <li class="rounded px-1 hover:bg-white transition-colors duration-300 <%= action_name == 'contact' && controller_name == 'static_pages' ? 'bg-white text-black' : 'hover:bg-opacity-20'%>">
            <%= link_to 'Contact', contact_path %>
          </li>
        </ul>
      </nav>
    </header>
    <main class="flex-grow">
      <%= yield %>
    </main>
  </body>
</html>
