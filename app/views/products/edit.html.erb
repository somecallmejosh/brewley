<div class="<%= page_container %>">
  <h1 class="text-4xl font-bold"><span class="text-gray-600">Editing:</span></h1>
  <%= render "form", product: @product %>

  <%= link_to "Show this product", category_product_path(@category, @product), class: button_class(theme: 'show', variant: 'small') %>
  <%= link_to "Back to products", category_products_path(@category, @product), class: button_class(theme: 'back', variant: 'small') %>
</div>
