<%= render(ProductCardComponent.new(payload: {
  image: category.image,
  name: category.name,
  link: category_products_path(category),
  chip_text: category.products_count,
  link_label: "View category"
})) %>
